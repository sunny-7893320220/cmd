list is the built in data type in python and it stores set of values, it can be different data-types

in go we used this concepts as the arrays and slices

And the best part is we can store the different types of data-types in a list.
example: 

list = ["saikrishna", 22, "hyderabad"]

The list has it own methods to perform the operations on the list.

The list is mutable, which can change
syntex 

list = [1, 2, 3, 4, 5]


the tuple is immutable, which is not changable
syntex 

tuple = (1, 2, 3, 4, 5)