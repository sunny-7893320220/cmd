sets are the built in data structure in python.

And it is a collection of unordered items {which means that index is not possiable.}

Each element in the set is unique. And the sets are the immutable.

in the sets 1. list 2. dictionrys are not possiable due to they are the mutable.


syntax 

set = {1, 2, 3,}


To create the empty set we have to use the 

            set()